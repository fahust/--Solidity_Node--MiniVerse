module.exports = {
  buyMV: async (ws, msg, wallet, jwt) => {
    //ws.send( await transaction.transaction(msg.contractAddress, wallet,[100],"buyMV") );
  },
  sellMV: async (ws, msg, wallet, jwt) => {
    //ws.send( await transaction.transaction(msg.contractAddress, wallet,[100],"sellMV") );
  },
};
